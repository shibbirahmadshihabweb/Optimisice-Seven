const testSelect = document.getElementById("test-select");
const testSelect2 = document.getElementById("test-select-2");
const choices = new Choices(testSelect, {
  searchEnabled: true,
  // searchEnabled: false,
});
const choices2 = new Choices(testSelect2, {
  searchEnabled: true,
  // searchEnabled: false,
});

